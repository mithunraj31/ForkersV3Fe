export const RESOURCE_TYPE = Object.freeze({
  GROUP: 'group',
  USER: 'user',
  CUSTOMER: 'customer',
  ROLE: 'role',
  DEVICE: 'device',
  EVENT: 'event',
  OPERATOR: 'operator'
})

export const SYSTEM_ROLE = Object.freeze({
  ADMIN: 'admin',
  USER: 'user'
})
